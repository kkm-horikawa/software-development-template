# 作業規則

このリポジトリでは、要求、設計、実装、確認、Pull Requestを別々の仕事にしない。同じ利用体験を最後まで引き継ぐ。

## 作業前

1. `README.md` と `docs/000_process.md` を読む。
2. テンプレートから新しい製品を始める場合は、最初のIssueより前に要求とシステムのサンプル本文を製品の現在像へ書き換え、サンプルのシナリオ、シーケンス、増分を削除する。
3. 対象のIssueを読み、困りごと、期待する体験、入口の入力と結果、完了条件を確かめる。
4. `docs/110_requirements` の関係するシナリオ、受入条件と具体例、要求、横断的な受入条件と、`docs/150_system` の現在像を読む。
5. Issue番号を含む `docs/210_increments/INC-<Issue番号>.md` を作り、要求からテストまでの対応を確かめる。
6. 既存のコードとテストを読む。現在の構造が変更の複雑さに見合うかも判断する。
7. 専用のGit worktreeとブランチで作業する。既定ブランチへ直接pushしない。

目的や完了条件が曖昧で、実装によって意味が変わる場合は、コードを書く前に一つの質問へ絞って確認する。

## 設計

- 体験を追加、変更、削除するときは、目的ごとのシナリオと受入例を現在の姿へ書き換える。画面や機能の一覧をシナリオにしない。
- 全体構造、シナリオに対応するシーケンス、データ構造へ影響する変更は、増分と同じPull Requestで現在の姿へ更新する。
- まず機能単位で分け、一緒に変わるものを近くへ置く。
- 独立して配備する処理は `backend/<サービス>`、独立して配布する利用先は `client/<製品>` に置く。各単位は依存関係、起動、検査を自分の中へ持つ。
- バックエンドとクライアントは `contracts` の公開契約で連携し、相手の内部実装を直接読み込まない。
- 業務の言葉で型とメソッドを名付ける。判断と状態変化は、そのデータを持つ型へ置く。
- 複雑な機能では、入口やデータベースなど外側から、利用の手順、業務の規則へ依存を向ける。
- 集約、値オブジェクト、リポジトリ、層は、業務規則の厚さと失敗時の影響が正当化するときだけ使う。
- 戻しにくい公開API、データ形式、データ移行は急いで決めない。内部の命名や分割は小さく直し続ける。
- 配置だけを理由に処理を別サービスへしない。個別配備、データ所有、障害分離が必要になった単位だけを取り出す。

詳しくは `docs/010_design-principles.md` を読む。

## 実装

- 公開入口は、利用の流れを同じ粒度のメソッドで上から順に呼ぶ。
- 呼ばれたメソッドでは一段だけ詳しくする。進行を示すコードへSQL、HTTP形式、細かな条件を混ぜない。
- 一つのメソッドは、一つの名前で説明できる責任を持つ。行数だけで分割しない。
- グローバル関数の羅列にせず、状態と依存は責任を持つ型へ閉じる。
- エラーは握り潰さず境界まで返し、利用者向けの表現への変換は境界で行う。
- コメントは「なぜ」を書く。「何をするか」は名前と構造で読めるようにする。

詳しくは `docs/020_code-structure.md` を読む。

## テストと完了

- テストは実装と同時に作る。
- 増分はIssue番号が決まってから作る。`INC-42.md` の検査設計は `ST-42-01`、`IT-42-01` のように増分の中で番号を付け、Issue作成時に予約しない。
- 実行テストは `test_sc001_ex01_...` のように現在のSCと受入例で名付ける。元Issueの番号を永久に持たせず、増分のST・ITから `テストファイル::テスト名` で実在するテストへ結ぶ。
- 受入テストは利用者または外部システムから観測できる入口と結果だけを見る。保存方式や内部メソッドの確認は結合テストへ分ける。
- 人が読む具体例はSCの `受入例.md`、実行形式のデータは使うテストのそばへfixture、factory、表駆動データとして置く。各テストが準備と後始末を持ち、別テストの実行順へ依存しない。
- 利用者に近い確認を優先し、受け入れ、システム、結合、単体の順で不足を考える。
- 自分のコードをモックせず、外部API、データベース、時刻などの境界だけを差し替える。
- `just check` を通し、中心の利用の流れを本物の入口から操作して結果を観測する。
- 未達は未達、未確認は未確認と書く。途中で難しくなった要求を勝手に対象外へ移さない。

Pull Requestの前にIssueと実装を突き合わせる。利用者が操作できる入口は変更前後を録画し、録画できない変更は理由を書く。Pull Requestには実際の入力、出力、画面、状態変化を記載する。

## Git

- 一コミットは一つの論理的変更にする。
- コミットメッセージと文書は、専門用語に短い日常語の説明を添えて日本語で書く。
- Issueに対応するコミットには `Github-Issue:#<番号>` をトレーラーとして付ける。
- Pull Requestを経由し、明示的な承認なしに既定ブランチへマージしない。
