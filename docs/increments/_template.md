# 増分: 短い名前

対応Issue: #番号
対応要求: REQ-番号、AC-番号

## 変える体験

誰が、どの入口で、何をして、何が起きるようにするかを書く。

## 外から見える振る舞いと確認

### SPEC-番号: 振る舞い [AC-番号]

入力、結果、状態変化を書く。

### ST-番号: システムテスト [SPEC-番号]

実際の入口へ何を渡し、何を観測すれば合格かを書く。

## 設計上の境界と確認

### AD-番号: 境界 [SPEC-番号]

変更をどこへ閉じ込め、依存をどちらへ向けるかを書く。

### IT-番号: 結合テスト [AD-番号]

どの実物同士をつなぎ、何を観測すれば境界が正しいと分かるかを書く。

## 実装の段

入口から同じ粒度で並ぶ処理と、次に一段詳しくなる処理を書く。

```text
入口.run
  → 段A
  → 段B
  → 段C
```

## 対象外

要求を決める時点で意識して作らないものを書く。未達や未確認をここへ移さない。
