# 目的名の受入条件と具体例

対応する利用目的: [SC-番号 目的名](README.md)

## AC-SC番号-01: 達成する状態 [REQ-番号] [SC-番号]

利用者または外部システムから見て、何を達成できれば受け入れるかを書く。技術的な実現方法は書かない。

### EX-SC番号-01: 具体例 [AC-SC番号-01]

- 前提:
- 入力:
- 観測:
- 方法: 自動または手動
- 実行物: 自動なら `テストファイル::テスト名`、手動なら `tests/acceptance/manual/SC-番号.md::EX-SC番号-番号`

## テストデータ

| 名前 | 意味 | 値 | 実行時の置き場所 |
|---|---|---|---|
|  |  |  |  |

人が読む値と期待結果はこの文書を正とする。実行形式のデータは、使うテストのそばへfixture、factory、表駆動データとして置き、各テストが準備と後始末を受け持つ。

## 手動確認

自動化しない具体例がある場合だけ、[`tests/acceptance/manual/_template.md`](../../../tests/acceptance/manual/_template.md)を写し、目的、入口、環境、準備、操作、観測、証跡、後始末を固定する。具体例の実行物は、確認書の見出しまで `ファイル::EX番号` で参照する。無ければ「なし」。

## 現在まだ受け入れないこと

シナリオから自然に期待されるが現在は提供しないことを書く。無ければ「なし」。
