# INC-1 HTTPから作業を始める

対応Issue: #1

> 作業記録アプリの変更例です。新しい製品では削除してください。

## 変更範囲

HTTPから作業開始を受け付ける変更例です。作業の一覧・終了・修正は含めません。

## 設計と確認

FastAPIは入力と応答、利用手順は作業の生成と保存順、SQLAlchemyは永続化を受け持ちます。業務の型はHTTPとDBを知りません。

外からは作業開始のHTTPケースを実行し、応答を確認します。保存境界は利用手順とSQLAlchemyを接続した結合テストで確認します。具体値はテストデータに置き、ここへ写しません。

## 結果のありか

テンプレートでの導入は [PR #2](https://github.com/kkm-horikawa/software-development-template/pull/2)。当時の実装はPRの差分から確認します。この文書は進め方の例であり、過去のPRを厳密に再構成したものではありません。
